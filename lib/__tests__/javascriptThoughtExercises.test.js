const assert = require('assert');
const javascriptThoughtExercises = require('../index.js');

describe('javascriptThoughtExercises', function () {
  it('has a test', function () {
    assert(false, 'javascriptThoughtExercises should have a test');
  });
});
